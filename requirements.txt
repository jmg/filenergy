Flask>=3.0,<4.0
Flask-SQLAlchemy>=3.1
Flask-Login>=0.6
Flask-Admin>=1.6
Werkzeug>=3.0
python-dotenv>=1.0
anthropic>=0.40
voyageai>=0.3
pypdf>=4.0
python-docx>=1.1
numpy>=1.26
stripe>=8.0
pyotp>=2.9
qrcode>=7.4
authlib>=1.3
Flask-Migrate>=4.0
fpdf2>=2.7
# WebAuthn / passkey ceremony — UI gracefully falls back to the stub flow
# when this isn't installed.
webauthn>=2.5
# Optional: install + run an `rq` worker to use FILENERGY_JOBS_BACKEND=rq
# rq>=1.16
# redis>=5.0
# Optional: SAML SSO (needs libxml2 + xmlsec system libs — Dockerfile includes them)
# python3-saml>=1.16
# Optional: Postgres engine
# psycopg2-binary>=2.9
# Optional: pgvector embeddings (Postgres only — needs the `pgvector` extension installed)
# pgvector>=0.2
